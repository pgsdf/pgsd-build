# Device         Mountpoint    FStype  Options  Dump  Pass#

# Root filesystem (/) is already mounted by the boot loader
# Do not add root mount entry - it causes boot failures on USB

# Memory filesystems (configured in rc.conf)
tmpfs           /tmp          tmpfs   rw,mode=1777  0  0
tmpfs           /var          tmpfs   rw            0  0
